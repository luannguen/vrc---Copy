# Environment Variables for VRC Frontend
# Copy this file to .env and update values as needed

# API Configuration
VITE_API_URL=http://localhost:3000
VITE_PUBLIC_API_KEY=vrc-api-2024-secure

# Development Server Configuration
VITE_FRONTEND_PORT=8081

# Environment Mode
VITE_NODE_ENV=production

# Additional Notes:
# - VITE_API_URL: Base URL to the backend server (proxy target)
# - API requests to /api/* will be proxied to VITE_API_URL/api/*
# - VITE_FRONTEND_PORT: Port for the Vite development server (default: 8081)
# - VITE_PUBLIC_API_KEY: API key for backend authentication
# - All variables prefixed with VITE_ are exposed to the browser
